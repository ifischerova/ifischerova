# Netlify build configuration.
# The site is an Eleventy + Tailwind static build: `npm run build` compiles the
# CSS and generates the static site into `_site`, which Netlify publishes.

[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"
